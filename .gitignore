# Sistema
.DS_Store

# Editor
.vscode/
.idea/

# Config local do Claude Code — fica só na máquina, nunca no repositório
.claude/
CLAUDE.md

# Segredos e dados locais do back-end (ver também server/.gitignore)
server/.env
server/.env.*
!server/.env.example
server/node_modules/
server/*.db
server/*.db-journal
server/*.db-wal
server/*.db-shm

# Fotos de produto enviadas pelo painel administrativo (conteúdo do
# usuário, não código-fonte — ver POST /api/admin/products/:id/photo em
# server/server.js)
server/img/products/

# Relatório de auditoria de segurança — uso interno, NÃO vai pro GitHub
# (contém o mapa dos pontos fracos do site; fica só na máquina).
docs/security-audit/
