# Segredos — nunca versionar
.env
.env.*
!.env.example

# Dependências
node_modules/

# Banco de dados local (usuários, sessões, pedidos)
*.db
*.db-journal
*.db-wal
*.db-shm

# Backups do banco (mesmo criptografados, são cópias de dados de clientes —
# ficam em disco/nuvem, nunca no git)
backups/
*.db.enc

# Sistema
.DS_Store
npm-debug.log*

# Mapa interno do código para o Claude Code — não é documentação do projeto,
# não deve ir para o GitHub
docs/CODEMAP.md
